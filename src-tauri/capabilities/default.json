{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for MIDI2Box.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
